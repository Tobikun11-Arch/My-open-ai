# Node modules (for both FE and BE)
node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variable files (for both FE and BE)
.env
.env.local
.env.*.local

# Build directories
/dist
/build
/public

# Coverage (for test reports)
/coverage

# OS-specific files
.DS_Store          # macOS
Thumbs.db           # Windows

# IDE and editor configurations
.vscode/            # VS Code
.idea/              # JetBrains IDEs

# Front-end specific
frontend/node_modules/  # Frontend node_modules if separated
frontend/.env            # Frontend .env if separated
frontend/dist/           # Frontend build files
frontend/build/          # Frontend build files

# Back-end specific
backend/node_modules/    # Backend node_modules if separated
backend/.env             # Backend .env file if separated
backend/dist/            # Backend build files (e.g., transpiled code)

# NPM package lock files (optional, depending on your setup)
# package-lock.json
# npm-shrinkwrap.json
